#include <stdio.h> // For standard input/output functions

int main() { // Start of the main function

    int f = 0, t = 1; // Declare and initialize two integer variables: f (false) and t (true)

    // Conditional statement to compare the value of 't' to 1
    if (t == 1) { 
        printf("true\n"); // Print "true" if the condition evaluates to true
    } 
    else {
        printf("false\n"); // Print "false" if the condition evaluates to false
    }

    return 0; // End of the program
}